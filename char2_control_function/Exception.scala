package com.book.scalaImpatient.char2_control_function

import java.net.MalformedURLException
import java.net.URL

/**
  * Created by wenchao on 2017/8/6.
  */
/*
class Exception {
  //第一个分支类型是double， 第二个是Nothing,因此if/else 表达式的类型是double
  val x=1
  if(x>0) x
  else  throw   new IllegalArgumentException("x shold be negative")
  //模式匹配
  try{
    process{new URL("http://horsnam.com/xxx.gif")}
  } catch{
    case _:MalformedURLException =>println("bad Url"+url)
    case ex:IOException =>ex.printStackTrace()
  }

  var in=new URL("http://www.xxx.com/xxx.gif").openStream()
  try{
    process(in)
  }
  finally {
    in.close()
  }

}
*/
